apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-api
  template:
    spec:
      containers:
      - name: "techsalad/csg-onboarding"
        env:
        - name: GO_ENV
          value: "development"